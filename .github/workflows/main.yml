name: Workflow1
on: [push, workflow_dispatch]
 jobs:
  run-on: ubuntu-latest
  steps:
   -name: get code
    uses: action/checkout@6
   -name: install dependencies
    run: npm ci
   -name: build code
    run: echo "build code"
